# Ch05 image placeholders — SOP §4.4
# 所有影像均為 placeholder，實際圖檔由後續編輯階段補入。

chapter: Ch05
total: 2
pending: 2
placeholders:
  - id: Ch05_S08_inheritance_tree
    slot_name: DataReader 繼承樹圖
    description: >
      單一根節點「DataReader（基類 · read 介面）」向下分出三支
      CSVReader / JSONReader / ParquetReader。節點以矩形 + 深綠實線連線，
      線條下方標註「override read()」。
    source_url: ""
    recommended_size: "1280x900 px"
    bounding_box: {x: 0.6, y: 1.3, w: 5.3, h: 5.0}
    status: pending
    asset_path: null
    style_notes:
      - 配色 #1B5E3F（根節點實心）+ 白底葉節點
      - 字體 Noto Sans CJK TC / Consolas（類別名）
      - 葉節點附小字註：CSVReader → pandas.read_csv、JSONReader → pandas.read_json、ParquetReader → pandas.read_parquet

  - id: Ch05_S16_chaining_flow
    slot_name: Method Chaining 流程圖
    description: >
      水平四節點流程：read(df) → drop_na() → normalize() → export()。
      每個節點上方以小字標「return self」，節點之間以深綠箭頭連接，
      下方以細虛線標註「Chaining = 單一物件貫穿全流程」。
    source_url: ""
    recommended_size: "1400x600 px"
    bounding_box: {x: 0.6, y: 1.3, w: 5.8, h: 4.8}
    status: pending
    asset_path: null
    style_notes:
      - 深綠 #1B5E3F 箭頭 + 灰 #808080 副標
      - 節點使用圓角矩形，字體 Consolas
      - 最右側節點留「→ self（可繼續 chain）」小標
