# Ch07 image placeholders — SOP §4.4
# 所有影像為 placeholder，實際圖檔由後續編輯階段補入。

chapter: Ch07
total: 3
pending: 3
placeholders:
  - id: Ch07_S05_memory_layout
    slot_name: ndarray 連續記憶體佈局對比
    description: >
      左半邊：Python list，畫出一串 pointer box（灰色）
      分別指向散落在 heap 各處的 PyObject（含型別 header）；
      右半邊：NumPy ndarray，畫出一整條連續的記憶體格子（深綠填色），
      每格大小相同（例如 8 bytes / float64），下方標註 CPU cache line 能一次載入整塊。
      上方副標：「散落 vs 連續 — 為什麼向量化能用 SIMD 一次吃多筆」。
    source_url: ""
    recommended_size: "1400x800 px"
    bounding_box: {x: 0.6, y: 1.3, w: 5.8, h: 5.0}
    status: pending
    asset_path: null
    style_notes:
      - 配色 #1B5E3F（ndarray 實心）+ #D3D3D3（list pointer 灰）
      - 字體 Noto Sans CJK TC / Consolas（型別名）
      - 右下角註記呼應 Ch1：CPU cache / SIMD / 連續記憶體

  - id: Ch07_S12_broadcasting_rule
    slot_name: Broadcasting 三步規則示意圖
    description: >
      以兩個 shape 為例（例 A: (2,1)、B: (3,)），依序展示三步驟：
      Step 1 右對齊維度 → Step 2 缺少的維度補 1 →
      Step 3 大小為 1 的維度沿該軸拉伸配對，最終得 shape (2,3)。
      每步以箭頭連接，拉伸動作用虛線延伸格子表達。
    source_url: ""
    recommended_size: "1400x700 px"
    bounding_box: {x: 0.6, y: 1.3, w: 5.8, h: 4.8}
    status: pending
    asset_path: null
    style_notes:
      - 深綠 #1B5E3F 實線格子、灰色 #808080 虛線拉伸
      - Step 編號用 pill 樣式（深綠底白字）
      - 底部一行小字：「Broadcasting 不複製資料，只改 stride」

  - id: Ch07_S17_axis_diagram
    slot_name: axis 方向圖（axis=0 / axis=1）
    description: >
      主體：一個 3 列 × 4 欄的矩陣方格。
      axis=0 標示：由下往上的垂直箭頭穿過每一欄，標「沿列方向聚合 → 每欄一個值」（輸出 shape (4,)）。
      axis=1 標示：由左往右的水平箭頭穿過每一列，標「沿欄方向聚合 → 每列一個值」（輸出 shape (3,)）。
      右上角貼 code：arr.mean(axis=0) / arr.mean(axis=1)。
    source_url: ""
    recommended_size: "1200x900 px"
    bounding_box: {x: 0.6, y: 1.3, w: 5.6, h: 5.0}
    status: pending
    asset_path: null
    style_notes:
      - 深綠箭頭 + 灰色網格
      - axis=0 與 axis=1 箭頭顏色區分（深綠 vs 炭灰）
      - 目的：解決「為什麼 axis=0 是欄不是列」的常見誤解
